# badges branch

Auto-generated download-stat assets live here so the scheduled
`download-stats` workflow can update the README badge without pushing to
`main` (which is protected). Do not merge this branch into `main`.

The README badge points at `assets/downloads-badge.json` on this branch.
