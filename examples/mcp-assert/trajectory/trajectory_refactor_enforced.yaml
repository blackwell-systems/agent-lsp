# Trajectory assertion: validates the /lsp-refactor skill protocol with phase enforcement.
# activate_skill must come first, deactivate_skill must come last.
# Blast radius (find_references) must precede simulation, which must precede apply.
name: lsp-refactor with phase enforcement follows enforced protocol
trace:
  - tool: activate_skill
    args:
      skill_name: lsp-refactor
      mode: block
  - tool: get_change_impact
    args:
      changed_files: ["fixtures/go/main.go"]
  - tool: preview_edit
    args:
      file_path: "fixtures/go/main.go"
      workspace_root: "fixtures/go"
      language: go
      start_line: 6
      start_column: 1
      end_line: 9
      end_column: 2
      new_text: "type Entity struct {\n\tName string\n\tAge  int\n}\n"
  - tool: apply_edit
    args:
      file_path: "fixtures/go/main.go"
  - tool: get_diagnostics
    args:
      file_path: "fixtures/go/main.go"
  - tool: run_build
    args:
      workspace_dir: "fixtures/go"
  - tool: run_tests
    args:
      workspace_dir: "fixtures/go"
  - tool: deactivate_skill
trajectory:
  - type: order
    tools: ["activate_skill", "get_change_impact", "preview_edit", "apply_edit", "run_build", "run_tests", "deactivate_skill"]
  - type: presence
    tools: ["activate_skill", "deactivate_skill", "get_change_impact", "preview_edit"]
  - type: absence
    tools: ["rename_symbol"]
  - type: args_contain
    tool: activate_skill
    args:
      skill_name: lsp-refactor
      mode: block
