# Trajectory assertion: validates the /lsp-understand skill protocol.
# get_server_capabilities must be called before go_to_implementation and find_callers.
# For Mode A (symbol): go_to_symbol must be the entry point.
# inspect_symbol (hover) must follow symbol location.
# find_callers direction=incoming must appear (Level 1 callers).
# find_callers direction=outgoing must appear (outgoing calls).
# find_references must follow find_callers (all reference sites).
# get_symbol_source is the final per-symbol step (retrieve implementation).
# Read-only — apply_edit and rename_symbol must NOT appear.
name: lsp-understand follows skill protocol
trace:
  - tool: go_to_symbol
    args:
      symbol_path: "main.Item"
  - tool: get_server_capabilities
    args: {}
  - tool: inspect_symbol
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
  - tool: go_to_implementation
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
  - tool: find_callers
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
      direction: "incoming"
  - tool: find_callers
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
      direction: "outgoing"
  - tool: find_references
    args:
      file_path: "fixtures/go/main.go"
      line: 6
      column: 6
      include_declaration: false
  - tool: get_symbol_source
    args:
      file_path: "fixtures/go/main.go"
      line: 6
trajectory:
  - type: order
    tools: ["go_to_symbol", "inspect_symbol", "find_references", "get_symbol_source"]
  - type: presence
    tools: ["go_to_symbol", "inspect_symbol", "find_references", "get_symbol_source"]
  - type: absence
    tools: ["apply_edit", "rename_symbol"]
