# Trajectory assertion: validates the /lsp-generate skill protocol.
# open_document must precede suggest_fixes (file must be loaded).
# suggest_fixes surfaces generator options before any execution.
# execute_command (or apply_edit) must follow suggest_fixes — not precede it.
# format_document and get_diagnostics must follow generation to verify output.
# Multiple execute_command calls must NOT be batched — one at a time only.
name: lsp-generate follows skill protocol
trace:
  - tool: open_document
    args:
      file_path: "fixtures/go/main.go"
      language_id: "go"
  - tool: suggest_fixes
    args:
      file_path: "fixtures/go/main.go"
      start_line: 6
      start_column: 1
      end_line: 6
      end_column: 1
  - tool: execute_command
    args:
      command: "gopls.implement_interface"
      arguments: []
  - tool: format_document
    args:
      file_path: "fixtures/go/main.go"
  - tool: get_diagnostics
    args:
      file_path: "fixtures/go/main.go"
trajectory:
  - type: order
    tools: ["open_document", "suggest_fixes", "execute_command", "format_document", "get_diagnostics"]
  - type: presence
    tools: ["open_document", "suggest_fixes", "get_diagnostics"]
