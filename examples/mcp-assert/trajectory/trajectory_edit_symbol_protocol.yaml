# Trajectory assertion: validates the /lsp-edit-symbol skill protocol.
# The skill resolves the symbol via find_symbol first.
# list_symbols is used to get the full range before apply_edit.
# apply_edit must follow symbol resolution — not precede it.
name: lsp-edit-symbol follows skill protocol
trace:
  - tool: find_symbol
    args:
      query: "Item"
  - tool: list_symbols
    args:
      file_path: "fixtures/go/main.go"
  - tool: apply_edit
    args:
      file_path: "fixtures/go/main.go"
      old_text: "type Item struct {"
      new_text: "type Item struct { // updated"
trajectory:
  - type: order
    tools: ["find_symbol", "apply_edit"]
  - type: presence
    tools: ["find_symbol", "apply_edit"]
  - type: absence
    tools: ["rename_symbol"]
